{
  "name": "jait",
  "version": "0.1.70",
  "private": true,
  "type": "module",
  "description": "Just Another Intelligent Tool — for Developers",
  "license": "MIT",
  "author": "JakobWl",
  "homepage": "https://jait.dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/Widev-e-U/Jait"
  },
  "keywords": [
    "ai",
    "agent",
    "developer-tools",
    "local-first",
    "terminal",
    "coding-agent"
  ],
  "workspaces": [
    "packages/*",
    "apps/*",
    "extensions/*",
    "skills/*"
  ],
  "scripts": {
    "build": "bun run --filter '!@jait/web' build",
    "dev": "bun run --filter '*' dev",
    "test": "vitest run",
    "test:e2e": "npm --prefix tests/e2e test",
    "test:watch": "vitest",
    "lint": "bunx oxlint .",
    "typecheck": "bun run --filter '*' typecheck",
    "release:check": "node scripts/check-release-version-source.js",
    "healthcheck": "bun run release:check && bun run typecheck && bun run build && bun run test && bun run lint",
    "clean": "rm -rf packages/*/dist apps/*/dist node_modules/.cache"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
